"""Module wiring.layout — placement et rendu via SVG assets.

Contient : placeur (`layout.py`), pipeline (`pipeline.py`), renderer SVG
(`renderer.py`), routage haut-niveau (`routing.py`, distinct du routeur A*
sous `..routing/`), catalogue de composants, chargeurs SVG, generateur
de breadboard.
"""
